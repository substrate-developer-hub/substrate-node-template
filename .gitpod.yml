# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/config-gitpod-file)
# and commit this file to your remote git repository to share the goodness with others.

tasks:
  - name: Update OS specifics
    command: sudo apt-get update && sudo apt install build-essential && sudo apt install --assume-yes git clang curl libssl-dev llvm libudev-dev make protobuf-compiler
  - name: Update Rust
    command: rustc --version && rustup default stable && rustup update && rustup update nightly && rustup target add wasm32-unknown-unknown --toolchain nightly && rustup show && rustup +nightly show
  - name: Cargo
    command: cargo build --release


